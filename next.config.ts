import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable React strict mode (optional but recommended for better error handling)
  reactStrictMode: true,
};

export default nextConfig;
